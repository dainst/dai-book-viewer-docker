version: '3.7'

services:
  dai-book-viewer:
    build:
      context: .
    ports:
      - "2223:80"
    volumes:
      - ./dai-book-viewer:/dai_book_viewer
      - ./sample:/usr/share/nginx/html/documents
      - ./annotations:/usr/share/nginx/html/annotations